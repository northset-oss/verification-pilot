# Retired PR receipt disclosure path

The PR disclosure subsystem is retired. Its CLI and library exports fail immediately and perform
no remote inspection, copy generation, credential lookup, or upstream mutation.

Historical pull-request text is not an active publication contract and is not regenerated. Current
publication is controlled only by explicit receipt-publication consent, the singular M-012
correction exception, and deterministic source-to-deployment validation.
